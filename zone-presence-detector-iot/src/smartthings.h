#pragma once
#include <Arduino.h>

// ===================================================================
//  МОДУЛЬ SMARTTHINGS: низкоуровневый HTTP-вызов сцены
//  Токен передаётся явно (без "Bearer", он добавляется здесь).
// ===================================================================

bool executeSceneHttp(const char *sceneId, const char *token);
